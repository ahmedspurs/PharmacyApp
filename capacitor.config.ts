import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PharmacyClint',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
